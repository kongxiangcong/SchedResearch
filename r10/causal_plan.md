# R10 Stage B pre-registration

Trigger: Stage A EXT128 completed first-session tests retain >5% recovery upper bounds after improved bounds and joint static search. This is enough to trigger a NEW causal experiment; it does not show recoverable gain. Wait for complete Stage A selection before running B. DMA32 is a predeclared contrast, not a selected positive variant.

Freeze one rule, no hyperparameter search: at EXT decision indices 64,65,66,67 until an action occurs, sample the current two EXT queue heads and both cluster local-DMA busy bits. Charge 2 cycles for each sample, even with no alternative or on quiet. If default is a read destined for a busy local DMA and the other cluster has a read head with idle local DMA, select that other head ONCE, charge an additional 8 cycles, and disable the observer. Otherwise stop after four samples. Do not inspect future phase, completion times, graph criticality or past baseline traces. Charge before service in the event engine; all subsequent endogenous events recomputed. Up to 16 added cycles (three unsuccessful 2-cycle samples + one 10-cycle sample), max one changed EXT choice. Queue head access and busy bits are assumed reference capabilities; no actual target/PPA claim.

Bounded state: saturating 7-bit decision counter through epoch67, one done bit; two cluster IDs/head valid/direction tags and two busy bits as a combinational snapshot. No per-request history, graph table, phase oracle or timer prediction. Counter update logic is included in the assumed snapshot/decision fee; unobserved decisions do not stall EXT. This is a research cost abstraction, not measured hardware area/energy. The action tries to avoid sending more EXT work toward a busy local stage while an idle stage could receive work. It discriminates supply-only/loose bound from one concrete causal starvation-avoidance opportunity, not all arbitration policies.

Strong baseline: freeze Stage A's per-hardware/per-condition validation-selected static unchanged, and run the rule on that identical graph. Fresh train8/validation8/test30+30 phases: seeds1110000/1120000/1130000/1140000. Train/validation are diagnostic out-of-sample runs for this fixed rule; there is no selectable rule or tuning. Always finish all test blocks, preserving losses and no-action observation cost. Backgrounds quiet/20%/35%. All phases disjoint from R9 and Stage A. Save all 360 test baseline/action detailed trace pairs, and numeric train/validation diagnostics.

Gate: in EACH hardware separately, BOTH nonextreme backgrounds and BOTH sessions must have mean net gain>=5%, 95% paired CI lower>0; 30 independent paired blocks per cell; EACH quiet session max per-block regression<=1%. Failure rejects this rule only. Simulated sessions are not real-device sessions. No minimum mechanism/PPA exploration unless every gate passes. No-action comparison is actual fixed-rule execution, not best-of oracle.

Independent verification: copied R9 trace validator changes its permitted charge set to {0,2,10} and counts actual changed choices separately from observation-only fees; max one changed choice remains. Independently reconstruct queue head and busy bits from trace event times, derive first qualifying sampled decision, verify all charged observations and rule choices. Independent trace auditing and summary/phase recomputation; faulty charge/choice detection. Frozen Stage A base model remains byte-identical R9.
